# ifndef __REGRA_10__
# define __REGRA_10__

// Insere os parâmetros formais na tabela de símbolos
void insereParamsTabSim(char porRef);

// Realiza a leitura dos valores para os identificadores passados
void leituraIdents();

# endif
